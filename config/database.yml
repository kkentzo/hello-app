default: &default
  adapter: mysql2
  encoding: utf8
  port: 3306
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  host: localhost
  database: hello_development
  username: root
  password:

test:
  <<: *default
  database: hello_test

production:
  <<: *default
  database: hello_production
